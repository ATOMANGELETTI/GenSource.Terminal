Follow the `update-app-info` skill at `.cursor/skills/update-app-info/SKILL.md`.

Ask whether this is a **version-only** bump or a **rebrand** (new app from the template). Collect the needed fields, then update identity/version files in lockstep. Do not invent names or versions, and do not demo-edit this template’s identity unless the user explicitly wants those changes applied.
