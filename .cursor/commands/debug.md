Prefer the `template-debugger` subagent at `.cursor/agents/template-debugger.md`.

Debug the current failure in this GenSource.Template repo (Tauri v2 + React + Rust). Capture the error, isolate frontend vs `src/configs/` vs `src-tauri/`, apply a minimal root-cause fix, and verify with the narrowest useful command. Respect placeholder and `mdoels` conventions.
