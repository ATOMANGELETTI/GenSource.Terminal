Follow the `create-skill-pro` skill at `.cursor/skills/create-skill-pro/SKILL.md`.

Scaffold a new Cursor Agent Skill under `.cursor/skills/<name>/SKILL.md`, ground paths in `references/project-context.md`, validate with `scripts/validate-skill.mjs`, and optionally add a matching `.cursor/commands/<name>.md` slash command.

Ask for the skill name, what it does, and when it should trigger if missing.
