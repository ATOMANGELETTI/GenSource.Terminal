Follow the `add-react-page` skill at `.cursor/skills/add-react-page/SKILL.md`.

Create a new React page under `src/app/pages/` (PascalCase filename), wire styles/types only as needed, and keep the template layout intact.

Ask for the page name and purpose if the user did not specify them.
