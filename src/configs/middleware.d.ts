import type { Plugin } from 'vite';
/**
 * Optional dev-server middleware. Logs requests when `DEBUG` is set.
 */
export declare function devServerMiddleware(): Plugin;
//# sourceMappingURL=middleware.d.ts.map