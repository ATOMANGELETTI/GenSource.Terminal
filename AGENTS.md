# AGENTS.md — GenSource.Template

Canonical project agent instructions live in
[`.cursor/AGENTS.md`](.cursor/AGENTS.md). Read that file at session start.

Agent context in this repo is **Cursor-native only**: `.cursor/rules/`
(`.mdc`), `.cursor/skills/`, `.cursor/commands/` (`.md`), `.cursor/agents/`
(`.md`), `.cursor/hooks.json` + `.cursor/hooks/`, `.cursor/mcp.json`, and
`.cursor/cli.json`.
