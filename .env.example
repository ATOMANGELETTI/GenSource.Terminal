# Copy to .env / .env.local and fill values. Never put real secrets here.
VITE_APP_NAME=
VITE_APP_ENV=

# Dev-only (Rust loads these when `tauri::is_dev()`). Packaged builds ignore .env.
# Do not prefix with VITE_ — the webview must not see these.
GENSOURCE_VAULT_PASSWORD=
GEMINI_API_KEY=
